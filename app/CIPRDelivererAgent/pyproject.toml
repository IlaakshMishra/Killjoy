[project]
name = "killjoy-ci-pr-deliverer"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "bedrock-agentcore",
    "boto3",
    "requests",
]

[tool.setuptools]
py-modules = ["main", "git_ops", "workflow_injector", "pr_builder"]

[project]
name = "killjoy-environment-mapper"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "bedrock-agentcore",
    "langchain-aws",
    "boto3",
]

[tool.setuptools]
py-modules = ["main", "scanner", "synthesizer"]

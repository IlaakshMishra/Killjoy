[project]
name = "killjoy-evaluator"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "bedrock-agentcore",
    "boto3",
    "mutmut",
    "pytest",
]

[tool.setuptools]
py-modules = ["main", "runner", "mutation", "sandbox"]

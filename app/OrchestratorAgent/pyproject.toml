[project]
name = "killjoy-orchestrator"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "bedrock-agentcore",
    "boto3",
]

[tool.setuptools]
py-modules = ["main", "guardrails", "pipeline"]
